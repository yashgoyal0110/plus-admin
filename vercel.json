{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "^/[^.]*$",  // Only match routes without a file extension
      "dest": "/index.html"
    }
  ]
}
